NODE_ENV=development
PORT=3001
MONGO_URI=mongodb://localhost:27017/hisabdar
# Use a long, randomly generated secret (at least 32 characters).
JWT_SECRET=replace-with-a-long-random-secret
# Comma-separated browser origins allowed to call the API. Required in production.
CORS_ORIGIN=http://localhost:3000
# Optional: enables authenticated AI reminders and insights without exposing the key to browsers.
GEMINI_API_KEY=
